# Test- und Lint-Abhaengigkeiten (nicht im Produktions-Image).
-r requirements.txt

pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.7

# Der Git-Hook ruft pre-commit bei jedem Commit auf. Stand es hier nicht, war
# ein frisch eingerichtetes venv commit-unfaehig: „No module named pre_commit",
# ohne dass irgendwo stuende, woher das Werkzeug kommen soll.
pre-commit==4.6.2
