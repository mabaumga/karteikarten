name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (zeigt nur die naechste Version, released nicht)"
        required: false
        default: false
        type: boolean

jobs:
  release:
    runs-on: ubuntu-latest
    concurrency: release
    permissions:
      id-token: write
      contents: write      # Tag + CHANGELOG-Commit zurueck nach main
      packages: write      # Push nach ghcr.io

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0           # volle Historie fuer semantic-release
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Abhaengigkeiten installieren
        run: pip install -r requirements.txt

      - name: Quality-Gate
        env:
          DJANGO_SETTINGS_MODULE: config.settings
          SECRET_KEY: ci-secret-key-not-for-production
        run: make check

      - name: python-semantic-release installieren
        run: pip install python-semantic-release

      - name: Dry Run — naechste Version anzeigen
        if: inputs.dry_run == true
        run: |
          echo "Aktuelle Version:"
          semantic-release version --print-last-released || echo "(Kein vorheriges Release)"
          echo ""
          echo "Naechste Version:"
          semantic-release version --print || echo "(Keine release-wuerdigen Commits)"

      - name: Release erstellen (Version + Tag + CHANGELOG)
        if: inputs.dry_run != true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: semantic-release version --push --tag --commit --changelog

      - name: Version auslesen
        if: inputs.dry_run != true
        id: version
        run: |
          VERSION=$(python -c "import karteikarten; print(karteikarten.__version__)")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Docker anmelden (ghcr.io)
        if: inputs.dry_run != true
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker Image bauen und pushen
        if: inputs.dry_run != true
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/karteikarten:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/karteikarten:latest
